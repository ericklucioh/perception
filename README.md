# 🧠 Perceptron Simples em Python

## 🚀 Rodar o projeto

```bash
python3 -m venv env     # cria o ambiente virtual
source env/bin/activate # ativa o ambiente virtual
pip install numpy       # instala dependências
python main.py          # executa seu script
```